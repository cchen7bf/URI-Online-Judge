#include <stdio.h>
#include <string.h>
int main() {

    int i=0;
    char texto[7];

    while (scanf("%s", texto)!=EOF) {
        while (1) {
           if (i==0) strcpy(texto, ) 
        }

    }
    return 0;
}
